# Repository Instructions

- Build the service in Python.
- Prefer FastAPI for HTTP endpoints.
- Keep business logic in `app/services` and API routes in `app/main.py`.
- Add concise docstrings to public functions.
- Include a `README.md` with setup and usage.
